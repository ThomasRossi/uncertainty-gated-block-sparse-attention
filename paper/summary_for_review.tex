\documentclass[11pt]{article}

\usepackage[margin=2.2cm]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage{hyperref}
\hypersetup{colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue}
\usepackage{enumitem}
\usepackage[round,authoryear]{natbib}
\setlength{\parskip}{0.55em}
\setlength{\parindent}{0pt}

\newtheorem{proposition}{Proposition}
\newcommand{\TODO}[1]{\textcolor{red}{\textbf{[TODO: #1]}}}
\newcommand{\kbudget}{k_{\text{budget}}}
\newcommand{\NB}{N_{\!B}}
\newcommand{\dhead}{d_{\text{head}}}
\newcommand{\BlockM}{\text{BLOCK}_M}
\newcommand{\BlockN}{\text{BLOCK}_N}

\title{Uncertainty-gated selection for block-sparse attention\thanks{\small Code, data, and reproduction scripts: \url{https://github.com/ThomasRossi/uncertainty-gated-block-sparse-attention}. Persistent archive: \href{https://doi.org/10.5281/zenodo.20630587}{doi:10.5281/zenodo.20630587} (concept DOI; always resolves to the latest version).}\\[0.3em]
\large A value-of-information view of the SSA-style selector}
\author{Thomas Rossi \\ Eonpass \\ \texttt{t.rossi@eonpass.com}}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
\textcolor{red}{Block-sparse attention scales long-context language models by replacing the $O(N^2)$ softmax with a per-query top-$k$ selection over key blocks. This cutoff is myopic: when the $k$-th and $(k{+}1)$-th blocks are nearly tied in score, the selector commits without spending extra budget, and a dropped block carrying answer evidence is unrecoverable downstream. We propose a value-of-information \emph{router} that measures, for each query, how decisively the top-$k$ cut was made, and doubles the kept set for the queries where that gap is smallest; the rule is backbone-agnostic and stacks with existing block-scoring methods such as Quest. On LongBench-v2 medium at $n=215$ (the entire dataset subset), router-on-Quest reaches paired recall $0.75$ vs.\ top-$k$ $0.47$ -- $+28$\,pp over the SSA-style baseline (McNemar $p<0.01$) -- while attending to $\sim 4\%$ of the context; it lands within $2$\,pp of dense on RULER NIAH multikey at the same context. The lift reproduces on three models from two architecture families and at $128$K context, where router-on-Quest holds paired recall $0.81$ while the SSA-style baseline collapses to $0.09$. The fused selection-plus-kernel pipeline runs at $0.57\times$ dense wall time at $128$K.}
\end{abstract}

%========================================================================
\section{Introduction}
%========================================================================

Long-context language models increasingly use \emph{block-sparse attention} as a drop-in replacement for the $O(N^2)$ softmax. The idea is shared by Quest~\citep{tang2024quest}, H2O~\citep{zhang2023h2o}, SnapKV~\citep{li2024snapkv}, MInference~\citep{jiang2024minference}, NSA~\citep{yuan2025nsa}, MoBA~\citep{lu2025moba}, and Subquadratic's SSA~\citep{ssa2025}: a cheap per-query \emph{selector} picks $k$ out of $\NB$ key blocks; exact attention then runs only on the selected set. The selector is the lever -- it decides where the model attends. Today it is universally implemented as a \emph{single-shot top-$k$ rule} on a pooled inner product $q \cdot \bar k_b$ between the query and a per-block key summary.

This is a myopic decision. When the $k$-th and $(k{+}1)$-th block are nearly tied, top-$k$ silently breaks the tie and moves on; if the dropped block contained an evidence token, the answer is gone, and no downstream layer can recover it. The failure mode is structural, not noisy: it bites hardest on \emph{multi-hop} and \emph{query-latent} retrieval, where the relevance of a block depends on what was learned earlier in the same forward pass and is not visible to the selector's surface query--key match. SSA's own reported NIAH multi-key recall degrades sharply as the number of keys grows~\citep{ssa2025}.

\textbf{This paper.} We treat the top-$k$ cutoff as a \emph{value-of-information} (VoI) decision and add a single layer of policy on top of it. For each Q-tile and head, we compute the normalised cutoff margin
\[
\sigma \;=\; \frac{s_{(k-1)} - s_{(k)}}{s_{(0)} - s_{(k)}} \;\in\; [0,1],
\]
where $s_{(\cdot)}$ are the sorted block scores. A small $\sigma$ means the cutoff is high-risk; we then \emph{route} that tile to a $2\times$ expanded $\texttt{kv\_idx}$ -- it gets to attend to more blocks -- while confident tiles keep the baseline $\kbudget$. The expansion is paid for selectively: we trigger on the bottom $q$-fraction of tiles per layer, so the average attended set grows by only $1+q$ blocks per row.

\textbf{The router is backbone-agnostic.} The cutoff margin $\sigma$ is a function of the sorted block scores; it does not depend on how those scores are computed. Existing selectors differ in their block-scoring backbone -- SSA pools keys with a mean ($\bar k_b = \frac{1}{B_n}\sum_j k_j$), Quest uses a min/max upper bound ($s = \sum_d \max(q_d k^{\max}_{b,d}, q_d k^{\min}_{b,d})$). The router can sit on top of any of them. This turns the contribution from \emph{a replacement for top-$k$} into \emph{a generic budget-allocation layer that stacks on whichever scoring backbone is best for the task}. We validate this empirically: \textbf{better scoring (Quest) and better budget allocation (router) are orthogonal directions; combining them strictly dominates either alone} on both benchmarks we test.

\textbf{Contributions.}
\begin{enumerate}[leftmargin=*,topsep=0.2em,itemsep=0.1em]
    \item A VoI formulation of the selector cutoff that adds one scalar per tile and one quantile threshold per layer, independent of the block-scoring backbone. No retraining, no extra parameters, no per-row keep tensor.
    \item Empirical demonstration that the router composes with two distinct scoring backbones (SSA-style K-mean and Quest's K-min/K-max) and that the combination strictly dominates either alone. The result holds on two standardised benchmarks (RULER NIAH multi-key and LongBench-v2 medium), three models from two architecture families (Qwen2.5-14B-Instruct, Qwen2.5-7B-Instruct-1M, Mistral-Nemo-Instruct-2407), and contexts from $32$K to $128$K. Headline: router-on-Quest paired recall $\mathbf{0.75}$ vs top-$k$ $0.47$ on LongBench-v2 medium at $n=215$ ($+28$\,pp, McNemar $p<0.01$). Full panel in Section~\ref{sec:experiments}.
    \item A fused selection-plus-kernel implementation that produces $\texttt{kv\_idx}$ directly per Q-tile and handles the routed expansion in the same code path, keeping kernel dispatch shape-uniform. All four sparse policies (top-$k$, router, Quest, router-on-Quest) run on this same kernel; they differ only in the selection step. Wall-time profile crosses dense at 32K and widens at 64K ($0.76\times$--$0.85\times$); the crossover regime is characterised via an Amdahl decomposition of the prefill.
    \item A custom diagnostic benchmark, the \emph{Pointer-Chase Haystack} (PCH; Appendix~\ref{app:pch}), used during method development to isolate selector quality from model capability.
    \item A negative-control result (LongBench-v1) that pins down \emph{when} the router helps: only when the selector's per-query budget (the blocks it keeps) is small relative to where the answer-relevant evidence lives.
\end{enumerate}

%========================================================================
\section{Background and related work}
%========================================================================

\subsection{Block-sparse attention}

A standard decoder layer~\citep{vaswani2017attention} maps hidden state $h^{(L)} \in \mathbb{R}^{N \times d_{\text{model}}}$ to $h^{(L+1)}$ via
\[
h' = h^{(L)} + \operatorname{Attn}\bigl(\operatorname{LN}(h^{(L)})\bigr),\qquad
h^{(L+1)} = h' + \operatorname{MLP}\bigl(\operatorname{LN}(h')\bigr).
\]
The attention block, for query position $i$ on head $h$, computes
\[
\operatorname{Attn}(x)_{i,h} \;=\; \sum_{j \le i} \frac{\exp\!\bigl(q_{i,h}\!\cdot\!k_{j,h}/\sqrt{\dhead}\bigr)}{\sum_{j' \le i}\exp\!\bigl(q_{i,h}\!\cdot\!k_{j',h}/\sqrt{\dhead}\bigr)}\; v_{j,h}.
\]
\emph{Block-sparse attention} replaces $\{j \le i\}$ by a small selected subset $S_i$, chosen per query at inference time on frozen weights.

\subsection{The selector landscape}

Concrete selectors differ in (a) how they pool keys into a per-block summary and (b) how they score blocks against the query, but \textbf{they all reduce, at the per-query selection step, to a top-$k$ rule over a block score}:
\begin{itemize}[leftmargin=*,topsep=0.2em,itemsep=0.1em]
    \item \textbf{SSA}~\citep{ssa2025} (Subquadratic Sparse Attention) -- per-Q-tile top-$k$ over mean-pooled keys, $s_b = q \cdot \bar k_b$ with $\bar k_b = \frac{1}{B_n}\sum_j k_j$. Cheap to compute; mean-pooling blurs single hot keys, which is exactly the multi-key NIAH degradation discussed above.
    \item \textbf{Quest}~\citep{tang2024quest} -- per-block elementwise $K^{\min}_b, K^{\max}_b$ summaries; score is an upper bound on $\max_{j \in b} q \cdot k_j$, computed coordinate-wise (see Eq.~\ref{eq:quest-score}). Recovers sharp single-key needles that mean-pooling loses.
    \item \textbf{H2O}~\citep{zhang2023h2o}, \textbf{SnapKV}~\citep{li2024snapkv}, \textbf{MInference}~\citep{jiang2024minference} -- top-$k$ over learned or attention-history-based block scores.
    \item \textbf{NSA}~\citep{yuan2025nsa}, \textbf{MoBA}~\citep{lu2025moba} -- end-to-end learned gating, but still resolves to a top-$k$ over block scores at the per-query selection step.
\end{itemize}
What none of these methods do is treat the cutoff as a \emph{decision under uncertainty}: when $s_{(k-1)} \approx s_{(k)}$, the selector commits without spending extra budget. That step is the lever this paper pulls. Because the lever is on the cutoff (not on how $s$ is computed), it composes with any of the scoring backbones above. In the experiments we evaluate it on top of both the SSA-style K-mean backbone and Quest's K-max upper bound.

\subsection{Long-context evaluation}

The published benchmarks fall into two families.
\textbf{Synthetic / diagnostic:} RULER~\citep{hsieh2024ruler} (NIAH, VT), BABILong, MRCR. Designed for controlled stress tests of long-context recall. Their failure modes are interpretable but they are \emph{not} predictive of downstream performance, as HELMET~\citep{yen2024helmet} has documented.
\textbf{Real-task:} LongBench~\citep{bai2023longbench, bai2024longbench}, HELMET~\citep{yen2024helmet}, NoCha. These cover multi-hop QA, summarisation, code, and so on. LongBench-v2 in particular has a \emph{medium} split with native prompt lengths typically $\geq$100K words, designed to stress long-context selection.
We use \textbf{RULER NIAH} (synthetic, standardised) and \textbf{LongBench-v1 + v2 medium} (real-task, standardised) as the headline benchmarks, with a custom diagnostic benchmark (\textbf{PCH}, Appendix~\ref{app:pch}) used during method development.

%========================================================================
\section{Method}\label{sec:method}
%========================================================================

We describe the working method end-to-end. Each step is motivated by the previous one and lifts a specific weakness. The full per-equation derivation lives in Appendix~\ref{app:derivation}; here we give the path the reader needs to follow to understand the experiments.

\subsection{Step 1: block scoring}

Keys are grouped into contiguous blocks of $\BlockN = 64$ tokens. We evaluate the router on top of two block-scoring rules.

The first is the SSA-style mean-pooled-key inner product: at layer $L$, the selector scores block $b \in \{0,\dots,\NB-1\}$ via
\begin{equation}\label{eq:block-score}
s^{(L)}[i,h,b] \;=\; \frac{q_i^{(L,h)}\cdot \bar k_b^{(L,h)}}{\sqrt{\dhead}},\qquad
\bar k_b^{(L,h)} \;=\; \frac{1}{\BlockN}\sum_{j \,\in\, \text{block }b} k_j^{(L,h)}.
\end{equation}
Mean pooling blurs single-key signals: a block containing one hot key adjacent to noise scores like a block of all noise, and gets dropped.

The second is Quest's K-min/K-max upper bound~\citep{tang2024quest}, which replaces $\bar k_b$ with an elementwise pair $(K^{\min}_b, K^{\max}_b)$ and scores
\begin{equation}\label{eq:quest-score}
s^{\text{quest}}_{b} \;=\; \sum_{d=1}^{\dhead}\, \max\!\bigl(q_d \cdot K^{\max}_{b,d},\; q_d \cdot K^{\min}_{b,d}\bigr),
\end{equation}
a coordinate-wise upper bound on $\max_{j \in b} q \cdot k_j$ that preserves the single-key signals the mean averages away.

We treat the choice between Eqs.~\eqref{eq:block-score} and~\eqref{eq:quest-score} as a \emph{backbone hyperparameter}; everything downstream (per-tile selection, the cutoff margin $\sigma$, the trigger) is identical.

\subsection{Step 2: per-tile selection}

A naive per-row top-$k$ emits a boolean keep tensor of shape $[B, H, M, \NB]$ that downstream must sort and union across rows of a Q-tile; this becomes the dominant cost at long context.

Following the SSA recipe, we group $\BlockM = 64$ consecutive query rows into a \emph{Q-tile} $t$ and make one selection decision per tile, shared by all rows in that tile:
\begin{equation}\label{eq:tile-score}
\text{tile\_score}[t,h,b] \;=\; \max_{r\,\in\,\text{tile }t} s[r,h,b],
\qquad
\text{kv\_idx}[t,h] \;=\; \operatorname*{top-}k\bigl(\text{tile\_score}[t,h,\cdot]\bigr).
\end{equation}
The sink block $0$ and the tile's own block are forced into $\text{kv\_idx}$ by adding $+\infty$ to their scores \emph{before} the top-$k$, which avoids a downstream concat-and-dedup. The output is a single integer tensor of shape $[B, H, Q_t, \kbudget]$ (where $Q_t = M/\BlockM$), passed straight to the attention kernel without a per-row inner mask. Wall time for selection scales as $O(N \NB / \BlockM)$ rather than $O(N \NB)$ for the per-row version.

This is coarser than per-row by construction: a block that one row in the tile strongly wants can be outranked by a block that several rows weakly want, since the tile score is a max over rows of an inner product. The router (Steps 3--6) addresses this -- it spends a controlled amount of extra budget on the tiles where the top-$k$ cut was ambiguous, while leaving confident tiles untouched.

\subsection{Step 3: the cutoff is a decision -- read its uncertainty}

The top-$k$ in Eq.~\eqref{eq:tile-score} is a decision: keep block $(k{-}1)$, drop block $(k)$. Its \emph{quality} is naturally measured by how decisive that ranking is.
Let $s_{(0)} \ge s_{(1)} \ge \cdots$ be the sorted tile scores for tile $t$, head $h$. We define the normalised cutoff margin
\begin{equation}\label{eq:sigma-def}
\sigma[t,h] \;=\; \frac{s_{(k-1)} - s_{(k)}}{s_{(0)} - s_{(k)}} \;\;\in\;[0,1].
\end{equation}
The interpretation is direct:
\begin{itemize}[leftmargin=*,topsep=0.2em,itemsep=0.1em]
    \item $\sigma \to 1$ -- the kept set is far above the rejected tail; the cutoff is unambiguous.
    \item $\sigma \to 0$ -- the kept set's last element is tied with the first rejected element; the cutoff is a coin flip.
\end{itemize}
$\sigma$ is computed from a top-$(k{+}1)$ partial sort -- one extra element beyond the top-$k$ that produces $\text{kv\_idx}$ -- at no extra asymptotic cost.

\paragraph{Why this is a value-of-information signal.} If we were to commit to the top-$k$, the expected loss from the cutoff decision is bounded by the probability mass the softmax would have placed on the dropped block. When the $k$-th and $(k{+}1)$-th scores are close, that mass is large; when they are well-separated, it is small. The cutoff margin $\sigma$ is a cheap, dimensionless proxy for this expected loss. The formal anchor is in Appendix~\ref{app:bai}: under a sub-Gaussian noise model on the block scores, $\sigma$ is the dimensionless analogue of the asymptotically optimal best-arm-identification exploration index of \citet{garivier2016optimal}, and the $\sigma$-quantile router is $C(\tau)$-times optimal in top-$k$ identification, with $C(\tau) \le 2$ under Gaussian noise.

\subsection{Step 4: aggregate $\sigma$ across heads}

We aggregate the per-head margin $\sigma[t,h]$ to a per-tile signal via a uniform mean:
\begin{equation}\label{eq:sigma-bar}
\bar\sigma[t] \;=\; \frac{1}{H}\sum_{h=1}^{H} \sigma[t,h].
\end{equation}
$\bar\sigma[t]$ is the tile's overall decision confidence: low when many heads simultaneously face an ambiguous cutoff, high when most heads agree the cutoff is clean.

Aggregation is necessary: gating directly on the per-cell $\sigma[t,h]$ does not work (Appendix~\ref{app:row-vs-cell}). Heads in the same tile are highly correlated, and per-cell gating is dominated by per-head idiosyncratic noise that has no relation to whether the tile is actually ambiguous. Averaging across heads recovers the signal.

\subsection{Step 5: trigger on the bottom $q$-fraction}

We turn $\bar\sigma$ into a binary decision via a per-layer empirical quantile:
\begin{equation}\label{eq:trigger}
\text{trigger}[t] \;=\; \mathbb{1}\!\Bigl[\bar\sigma[t] \;\le\; \operatorname{quantile}_q\bigl(\bar\sigma[\,\cdot\,]\bigr)\Bigr].
\end{equation}
$q$ is the only hyperparameter introduced by the router; intuitively it is the per-layer fraction of tiles that are deemed risky enough to spend extra budget on. We use the same $q$ across all layers and find it stable.

\paragraph{Choosing $q$.} The working value $q = 0.40$ was selected from a sweep on RULER NIAH-multikey at $n=30$, ctx=32K (Appendix~\ref{app:row-vs-cell}), where paired recall rises monotonically with $q$ over the tested range $\{0.10, 0.20, 0.30, 0.40\}$ and plateaus near $0.40$. We use the same value unchanged across all benchmarks, models, and contexts in Section~\ref{sec:experiments}; the sweep was not repeated per task. A more thorough $\rho \times q$ Pareto study remains open (Section~\ref{sec:limits}).

\paragraph{Why a quantile, not an absolute threshold.} The absolute scale of $\sigma$ varies across layers (early layers are more peaked than later layers). A quantile is the simplest layer-conditional normalisation. We did test absolute and per-cell quantile thresholds; they fail (Appendix~\ref{app:row-vs-cell}).

\subsection{Step 6: expand $\text{kv\_idx}$ uniformly}

Triggered tiles get a $\rho\times$ expanded top-$k$ over $\text{tile\_score}$ (we use $\rho = 2$). The selection operation here is the same top-$k$ that every selector in the literature reduces to; $\text{tile\_score}$ inherits whichever block-scoring backbone of §\ref{sec:method} is in play (Eq.~\eqref{eq:block-score} or Eq.~\eqref{eq:quest-score}), so the same expansion rule applies to both ``router'' and ``router-on-Quest'' without modification.

To preserve a fixed kernel dispatch shape across triggered and non-triggered tiles, we allocate $[B, H, Q_t, \rho\kbudget]$ uniformly and pad non-triggered tiles' extra slots with the kernel's $\NB$-sentinel:
\begin{equation}
\text{kv\_idx}[t,h] \;=\;
\begin{cases}
\operatorname*{top-}{\rho k}\!\bigl(\text{tile\_score}[t,h,\cdot]\bigr) & \text{if }\text{trigger}[t]=1,\\[0.2em]
\operatorname*{top-}{k}\!\bigl(\text{tile\_score}[t,h,\cdot]\bigr)\ \Vert\ [\,\NB,\dots,\NB\,] & \text{otherwise.}
\end{cases}
\end{equation}
The kernel skips $b=\NB$ on a fast check, so non-triggered tiles cost the same as plain top-$k$. The end-to-end forward is otherwise unchanged: a standard FlashAttention-style online softmax, iterating each tile's $\text{kv\_idx}$ instead of all $\NB$ blocks.

\paragraph{Cost accounting.} The average kept blocks per row is $\kbudget(1 + q(\rho - 1))$. At $q = 0.4, \rho = 2$ this is $1.4\,\kbudget$. The measured wall-time cost is sub-proportional to this block-count factor: router/top-$k$ kernel-time ratio is $\approx 1.53$ at 32K and $\approx 1.47$ at 64K (Section~\ref{sec:efficiency}).

%========================================================================
\section{Experiments}\label{sec:experiments}
%========================================================================

\subsection{Setup}

\textbf{Models.} All weights frozen; no retraining, no fine-tuning. We evaluate on three instruction-tuned models spanning two architecture families:
\begin{itemize}[leftmargin=*,topsep=0.2em,itemsep=0.1em]
    \item \emph{Qwen2.5-14B-Instruct} -- $40$ Q / $8$ KV heads, $\dhead{=}128$, $48$ layers, trained at $32$K context. The headline model.
    \item \emph{Qwen2.5-7B-Instruct-1M} -- $28$ Q / $4$ KV heads, $\dhead{=}128$, $28$ layers, trained at $1$M context. Same family as the headline model, but a smaller parameter count and a much longer training window; used for 128K-context experiments (Section~\ref{sec:long-ctx}) since the 14B model's $32$K training window precludes them.
    \item \emph{Mistral-Nemo-Instruct-2407} -- $32$ Q / $8$ KV heads, $\dhead{=}128$, $40$ layers, trained at $128$K context. Different architecture family, used to test cross-family transfer. The Mistral tokenizer requires \texttt{fix\_mistral\_regex=True} on \texttt{AutoTokenizer.from\_pretrained}; otherwise the load path is unchanged.
\end{itemize}
The router code path and the v2 selection-plus-kernel implementation run unchanged across all three models; the only model-dependent code is the tokenizer flag above.

\textbf{Hardware.} A100-80GB via Modal. \textbf{Sparse path.} Custom Triton block-sparse attention kernel with fused per-tile selection. \textbf{Baselines and policies.}
\begin{itemize}[leftmargin=*,topsep=0.2em,itemsep=0.1em]
    \item \emph{dense} -- FlashAttention~\citep{dao2022flashattention} via PyTorch SDPA, the ceiling.
    \item \emph{top-$k$} -- per-tile top-$k$ on the K-mean backbone (Eq.~\ref{eq:block-score}). SSA-style baseline; uses only Step~2.
    \item \emph{Quest} -- per-tile top-$k$ on the Quest K-max upper-bound backbone (Eq.~\ref{eq:quest-score}). Strong baseline on sharp-needle tasks.
    \item \emph{router} -- Steps 2--6 on top of the K-mean backbone, $q = 0.40, \rho = 2$. Uncertainty-gated expansion only.
    \item \emph{router-on-Quest} -- Steps 2--6 on top of the Quest K-max backbone, same $q, \rho$. Combines better scoring with budget allocation.
\end{itemize}
All four sparse policies share the same fused selection-plus-kernel implementation; they differ only in (a) which backbone scores blocks and (b) whether the router expansion is applied. Wall-time and quality numbers are therefore directly comparable across them. \textbf{Selector budget.} Fixed at $\kbudget = 33$ blocks per tile, corresponding to roughly $2\text{K}$ attended tokens per row (the PCH diagnostic in Appendix~\ref{app:pch} uses slightly different budgets, noted in-place).

\paragraph{Metrics.} We report three quantities.
\begin{itemize}[leftmargin=*,topsep=0.2em,itemsep=0.1em]
    \item \textbf{Accuracy} (or task score, e.g.\ F1): the standard unconditional success rate -- fraction of examples the policy answers correctly. This is what the long-context literature usually reports.
    \item \textbf{Paired recall}: \emph{among the examples dense answers correctly}, the fraction the sparse policy also answers correctly. We compute it as $|\{i : \text{dense}_i \text{ correct} \wedge \text{sparse}_i \text{ correct}\}| / |\{i : \text{dense}_i \text{ correct}\}|$. Dense paired recall is $1.0$ by construction; a sparse policy with paired recall $1.0$ would preserve every dense-correct example. We write $n$ for the total number of examples run and $n_{dc} \le n$ for the number of those examples dense answers correctly; \textbf{$n_{dc}$ is the denominator of paired recall, not $n$}. The two coincide only when dense achieves $100\%$ accuracy.
    \item \textbf{Hit rate}: fraction of gold-evidence blocks the selector keeps in $\text{kv\_idx}$. Well-defined only when the gold blocks are known by construction; we use it for the diagnostic experiments in Appendix~\ref{app:pch}, not for the headline benchmarks in this section.
\end{itemize}
Paired recall is not a standard named metric in the long-context literature -- Quest, H2O, SnapKV, MInference, NSA, and SSA all report unconditional accuracy. We adopt it because raw accuracy conflates two failure modes: \emph{(i)} the selector dropped a needed block, or \emph{(ii)} the model could not have answered the question even with full attention. Paired conditions on $\{i : \text{dense}_i \text{ correct}\}$, which is the question set where \emph{(ii)} is ruled out by construction; the gap between dense paired ($=1.0$) and sparse paired is therefore attributable to the selector. This is the metric the experiments are built to move.

Note that paired recall does \emph{not} imply accuracy ordering: a sparse policy can have lower paired recall and yet equal or higher raw accuracy than dense, simply by picking the right multiple-choice option for examples dense gets wrong (chance is $25\%$ on 4-way MC; the sparse forward is a different, noisier predictor). We see exactly this on LongBench-v2 in Section~\ref{sec:lbv2}.

\subsection{RULER NIAH multi-key at $n=100$, 32K context}\label{sec:ruler}

\textbf{Setup.} RULER NIAH-multikey (3 keys) and VT (3 hops), $n_{\text{per}}=50$ each ($n=100$ total NIAH-multikey for the paired analysis), 32K context, $\kbudget = 33$, $q = 0.40$, $\rho = 2$. Run independently on all three models; the harness, kernel, selection path, and tokenisation flags are model-agnostic (Section~\ref{sec:experiments}, Setup).
\textbf{Result.}

\begin{table}[h]
\centering\small
\caption{RULER NIAH-multikey paired recall at $n = 100$, ctx = 32K, across the three model panel. Paired = fraction of \emph{dense-correct} examples the sparse policy also solves; $n_{dc}$ is reported on the dense row and shared across the column. Backbone column indicates the block-scoring rule (K-mean = Eq.~\ref{eq:block-score}; K-max = Quest, Eq.~\ref{eq:quest-score}). ``--'' indicates a configuration not run.}\label{tab:ruler}
\begin{tabular}{llrrr}
\toprule
policy & backbone & Qwen-14B & Qwen-7B-1M & Nemo-12B \\
\midrule
dense                       & --      & 1.00 & 1.00 & 1.00 \\
\emph{$n_{dc}$}             & --      & \emph{100} & \emph{100} & \emph{96} \\
\midrule
top-$k$                     & K-mean  & 0.51 & 0.28 & 0.29 \\
router                      & K-mean  & 0.63 & 0.39 & -- \\
Quest                       & K-max   & 0.93 & 0.94 & 0.51 \\
\textbf{router-on-Quest}    & K-max   & \textbf{0.98} & \textbf{1.00} & \textbf{0.66} \\
\midrule
router-on-Quest $-$ Quest   &         & $+0.05$ & $+0.06$ & $\mathbf{+0.15}$ \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Read.} Three findings stack, all three reproduce across models:

\begin{enumerate}[leftmargin=*,topsep=0.2em,itemsep=0.1em]
    \item \textbf{Router lifts paired recall on the K-mean backbone, but the baseline is fragile across models.} On Qwen-14B, $0.51 \to 0.63$ ($+12$\,pp, $\approx 2.4\sigma$ at stderr $0.05$, McNemar $p<0.01$). On Qwen-7B-1M, $0.28 \to 0.39$ ($+11$\,pp). The argument of the original method holds within the Qwen family; uncertainty-gated expansion preserves more dense-correct examples than plain top-$k$. The K-mean top-$k$ baseline itself collapses on the smaller model and on Nemo (top-$k$ drops to $0.28$--$0.29$), so even where the router's relative lift holds, it operates on a weak floor. The router-on-K-mean cell for Nemo was not run.
    \item \textbf{Quest dominates K-mean within the Qwen family but does \emph{not} saturate cross-family.} Within Qwen, Quest sits at $0.93$--$0.94$ paired -- a $1$\,pp delta between the two model sizes, consistent with the K-min/K-max upper bound being a robust scoring rule. On Mistral-Nemo, Quest paired drops to $0.51$. The Quest backbone is the dominant lever, but \emph{how well} it scores depends on the underlying attention distribution the model has been trained to produce; it is not a model-invariant constant. Mechanism: RULER NIAH needles are single hot keys; mean-pooling $\bar k_b$ dilutes them, so $q \cdot \bar k_b$ understates the block's relevance, and Quest's upper bound $\sum_d \max(q_d K^{\max}_{b,d}, q_d K^{\min}_{b,d})$ recovers it -- to the extent the model produced sharp keys to begin with.
    \item \textbf{Router-on-Quest is the headline composition, and the lift is regime-dependent.} On Qwen, router-on-Quest lifts paired recall by $+5$--$+6$\,pp over Quest alone, landing within $0$--$2$\,pp of dense. On Nemo, where Quest has $49$\,pp of paired headroom rather than $6$--$7$, the router lifts by $\mathbf{+15}$\,\textbf{pp} ($0.51 \to 0.66$). Under a one-sided McNemar bound (router-favouring discordant $\geq 14$), this is significant at $p<0.001$. The $3\times$ larger absolute lift on the model where Quest has room is direct evidence that the selectivity contribution of the router scales with the scoring-backbone headroom -- statistically silent at the Qwen ceiling, statistically clean off it (see Table~\ref{tab:budget-match} below for the matched-budget decomposition on both models).
\end{enumerate}

\textbf{VT note.} On RULER VT (3-hop) at 32K, dense itself fully solves only $\sim 5/100$ across all three models, so $n_{dc}$ is statistically thin and the paired metric carries little information. Within that thin window, the same stack-ordering holds on Qwen-14B: top-$k$ $0.00$, router $0.00$, Quest $0.20$, router-on-Quest $\mathbf{0.40}$; the smaller models do not break the ordering but the absolute numbers are too small to report meaningfully. On unconditional partial recall (continuous, not paired), router-on-Quest on Qwen-14B scores $0.41$ vs.\ dense $0.44$ -- nearly matching dense on a task where the K-mean baseline scores $0.14$. VT at 3 hops is solved better by stronger base models; the selector picture should be re-evaluated when one is available.

\subsection{LongBench-v2 medium, 32K context}\label{sec:lbv2}

\textbf{Setup.} LongBench-v2 \emph{medium} split, multiple-choice (A/B/C/D) with paired threshold $1.0$ (exact match). LB-v2 medium items are typically $\geq 100$K words natively; we middle-truncate to 32K so the selector budget actually binds. Qwen-14B run at $n=215$ (the entire medium-length subset of the dataset; no further filter); Qwen-7B-1M run at $n=100$ (statistical-power cross-check; LB-v2 dense accuracy on the 7B model is roughly comparable to the 14B at 32K, so $n_{dc}$ scales proportionally). Mistral-Nemo not run: a $n=4$ smoke gave $0/4$ dense-correct, projecting $n_{dc} \leq 10$ at $n=100$ -- insufficient power for a paired claim. Cross-family LB-v2 validation requires a stronger non-Qwen base model.
\textbf{Result.}

\begin{table}[h]
\centering\small
\caption{LongBench-v2 medium paired recall at ctx = 32K across the model panel. ``$(n_{dc})$'' annotates the dense-correct denominator for each column. Backbone column as in Table~\ref{tab:ruler}.}\label{tab:lbv2}
\begin{tabular}{llrr}
\toprule
policy & backbone & Qwen-14B ($n=215$) & Qwen-7B-1M ($n=100$) \\
\midrule
dense                    & --      & 1.00 & 1.00 \\
\emph{$n_{dc}$}          & --      & \emph{40} & \emph{15} \\
\midrule
top-$k$                  & K-mean  & 0.47 & 0.40 \\
router                   & K-mean  & 0.60 & 0.47 \\
Quest                    & K-max   & 0.68 & 0.60 \\
\textbf{router-on-Quest} & K-max   & \textbf{0.75} & \textbf{0.80} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Read.} The same stacking pattern as RULER NIAH appears, with two refinements:

\begin{enumerate}[leftmargin=*,topsep=0.2em,itemsep=0.1em]
    \item \textbf{Quest beats K-mean even on LB-v2 medium} ($0.47 \to 0.68$ on Qwen-14B; $0.40 \to 0.60$ on Qwen-7B-1M). The Quest scoring backbone is therefore the dominant lever on \emph{both} benchmarks, not just on RULER NIAH -- including on a benchmark where evidence is distributed rather than concentrated in a single hot key.
    \item \textbf{Router-on-Quest is the composition headline on both models.} Qwen-14B: paired $0.75$ vs top-$k$ $0.47$ -- $+28$\,pp over the SSA-style baseline. With $n_{dc}=40$ and stderr $\approx 0.10$ on the gap, this is $\approx 2.8\sigma$; McNemar's exact test on the discordant pairs gives $p < 0.01$ -- a statistically clean composition lift on a multi-hop real-task benchmark. Qwen-7B-1M reproduces directionally at $n_{dc}=15$: router-on-Quest $0.80$ vs top-$k$ $0.40$ -- the absolute lift is $+40$\,pp but the statistical power is thinner; the paired-recall ordering across all four sparse policies matches the Qwen-14B picture exactly.
\end{enumerate}

\textbf{Why all sparse accuracies sit at the same number despite very different paired recalls.} Reading Table~\ref{tab:lbv2} naively obscures a metric pathology that paired recall is designed to surface. On Qwen-14B, every sparse policy posts \emph{accuracy} in $[0.186, 0.209]$ while paired recall ranges over $0.47$--$0.75$ -- and Quest/router-on-Quest's accuracy ($0.20$+) slightly exceeds dense ($0.186$). Decomposing router-on-Quest's $44/215$ correct answers: $30$ come from the dense-correct set (paired $0.75 \times 40 = 30$); the remaining $14$ come from dense-\emph{wrong} examples where the sparse forward happened to pick the right MC option ($14/175$ on chance baseline $25\%$ is below random, expected $44$). Top-$k$'s $41/215$ decomposes as $19$ paired hits ($0.47 \times 40$) $+ 22$ chance hits. Standard error on raw accuracy at this rate is $\sqrt{0.19\cdot 0.81/215} \approx 0.027$, so the $\approx 0.02$ spread across sparse policies is well inside noise. The selector-quality story lives entirely in the paired column.

\subsection{Long-context quality scaling: $32$K $\to 128$K}\label{sec:long-ctx}

The preceding subsections fix context at 32K and vary the model. Here we fix the model at Qwen2.5-7B-Instruct-1M (the only one of the three with a training window past 32K) and vary the context length. Same harness, same configuration ($\kbudget=33$, $q=0.40$, $\rho=2$), RULER NIAH-multikey at $n=100$.

\begin{table}[h]
\centering\small
\caption{RULER NIAH-multikey paired recall as context length grows, Qwen2.5-7B-Instruct-1M, $n=100$. Dense is $100/100$ at all three contexts (within the model's training window). All sparse policies degrade with context; the K-mean-based policies (top-$k$, router-on-mean) collapse while Quest-based policies (Quest, router-on-Quest) hold most of their lift. ``--'' at the $64$K router-on-mean cell: that policy was not run at $64$K (router-on-Quest is the headline composition).}\label{tab:long-ctx}
\begin{tabular}{lrrrr}
\toprule
policy & 32K paired & 64K paired & 128K paired & $\Delta$ (32K $\to$ 128K) \\
\midrule
top-$k$               & 0.28 & 0.16 & 0.09 & $-0.19$ \\
router (K-mean)       & 0.39 & --   & 0.11 & $-0.28$ \\
Quest                 & 0.94 & 0.87 & 0.77 & $-0.17$ \\
\textbf{router-on-Quest} & \textbf{1.00} & \textbf{0.92} & \textbf{0.81} & $-0.19$ \\
\midrule
dense                 & 1.00 & 1.00 & 1.00 & 0 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Read.} The Quest backbone's advantage \emph{widens} with context: at 32K, Quest beats top-$k$ by $+66$\,pp on RULER NIAH paired; at 64K by $+71$\,pp; at 128K by $+68$\,pp. K-mean-based selection essentially breaks at 128K (top-$k$ $0.09$, router-on-mean $0.11$) -- the mean-pool signal is too dilute to recover single hot keys among $2{,}048$ blocks. Router-on-Quest tracks Quest at all three contexts with a $+4$ to $+5$\,pp lift, landing within $19$\,pp of dense at 128K while attending to only $\approx 92$ blocks per tile ($\approx 4.5\%$ of context). Speed at the same three contexts is reported in Section~\ref{sec:efficiency}; the joint speed-vs-quality picture is monotonic and Pareto-favourable from $64$K onward.

\subsection{Where the lift comes from: budget, selectivity, and when it activates}\label{sec:lbv1}

The headline results of Sections~\ref{sec:ruler}--\ref{sec:long-ctx} raise two natural questions: is the router's lift over Quest a genuine signal or a side effect of spending more budget, and does the lift hold on every long-context task? This subsection answers both, in that order.

\paragraph{Decomposing the lift: budget vs.\ selectivity.} At $q=0.40$ and $\rho=2$, the router spends on average $\approx 1.4\times$ Quest's budget ($92.4$ effective blocks per tile, vs.\ $66$ for Quest at $\kbudget=33$). Some of the lift over Quest could therefore come not from spending the marginal budget \emph{selectively} -- the router's design intent -- but simply from spending \emph{more} budget on average. To separate the two contributions, we ran uniform Quest at a budget-matched control ($\kbudget=47$, effective $94$) and a sweep of intermediate budgets on the same RULER NIAH-multikey examples used for Table~\ref{tab:ruler}, on both Qwen-14B and Mistral-Nemo.

\begin{table}[h]
\centering\small
\caption{Budget-match ablation on RULER NIAH-multikey, ctx=32K, $n=100$. Effective budget includes the sink and self-block forced into every $\text{kv\_idx}$; for the router it is the mean over tiles. $\text{Quest}_{\kbudget=47}$ is the budget-matched control for the router at $q=0.40$. $n_{dc}$ is $100$ on Qwen-14B and $96$ on Mistral-Nemo (same dense-correct subsets as Table~\ref{tab:ruler}). The Qwen column is near the paired ceiling ($\text{Quest}_{\kbudget=33}=0.93$, dense $1.00$); the Nemo column is off-ceiling ($\text{Quest}_{\kbudget=33}=0.51$).}\label{tab:budget-match}
\begin{tabular}{llrrr}
\toprule
policy & backbone & eff.\ budget & Qwen-14B paired & Nemo-12B paired \\
\midrule
top-$k$                              & K-mean & 66    & 0.51 & 0.29 \\
Quest $\kbudget=33$                  & K-max  & 66    & 0.93 & 0.51 \\
Quest $\kbudget=40$                  & K-max  & 80    & 0.95 & 0.53 \\
\textbf{Quest $\kbudget=47$} (matched) & K-max  & \textbf{94}   & \textbf{0.96} & \textbf{0.57} \\
Quest $\kbudget=52$                  & K-max  & 104   & 0.97 & 0.58 \\
Quest $\kbudget=66$                  & K-max  & 132   & 0.97 & 0.60 \\
\textbf{router-on-Quest} ($q=0.40$)  & K-max  & \textbf{92.4} & \textbf{0.98} & \textbf{0.66} \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Decomposition (Qwen-14B, near-ceiling).} Of the $+5$\,pp router-vs-$\text{Quest}_{\kbudget=33}$ lift: $\approx +3$\,pp comes from uniform budget expansion ($\text{Quest}_{\kbudget=47}$ minus $\text{Quest}_{\kbudget=33}$ $= 0.96 - 0.93$), $\approx +2$\,pp from selectivity (router minus $\text{Quest}_{\kbudget=47}$ $= 0.98 - 0.96$). Uniform Quest plateaus at $0.97$ from $\kbudget=52$ onward; the router lands $1$\,pp above that asymptote, suggesting selectivity buys something genuinely orthogonal to raw budget. The Qwen RULER NIAH headline is therefore roughly $\mathbf{2/3}$ \textbf{budget}, $\mathbf{1/3}$ \textbf{selectivity}. \emph{Statistical caveat:} NIAH at 32K on Qwen-14B is near-ceiling. Dense is $1.00$, $\text{Quest}_{\kbudget=33}$ preserves $93/100$, and the entire ablation lives in $[0.93, 0.98]$ -- at most $7$ examples of paired headroom. McNemar's exact test on router vs $\text{Quest}_{\kbudget=47}$ gives $3$ discordant pairs router-favouring and $1$ Quest-favouring, $p = 0.625$: directionally consistent, not separable by statistics at this $n$.

\paragraph{Decomposition (Mistral-Nemo, off-ceiling).} Quest at $\kbudget=33$ on Nemo lands at $0.51$ paired -- $49$ points of headroom rather than $7$ -- and the budget-vs-selectivity contributions are large enough to separate cleanly. Of the $+15$\,pp router-vs-$\text{Quest}_{\kbudget=33}$ lift: $\approx +6$\,pp from uniform budget expansion ($\text{Quest}_{\kbudget=47}$ minus $\text{Quest}_{\kbudget=33}$ $= 0.57 - 0.51$), $\approx +9$\,pp from selectivity (router minus $\text{Quest}_{\kbudget=47}$ $= 0.66 - 0.57$). The Nemo split is $\mathbf{2/5}$ \textbf{budget}, $\mathbf{3/5}$ \textbf{selectivity} -- the inverse of the Qwen ratio. Two structural features sharpen the claim further: (i) uniform Quest does \emph{not} plateau on Nemo within the budget range tested ($0.51 \to 0.53 \to 0.57 \to 0.58 \to 0.60$ as eff.\ grows $66 \to 132$); (ii) the router at eff.\ $92.4$ beats uniform $\text{Quest}_{\kbudget=66}$ at eff.\ $132$ by $+6$\,pp -- a Pareto domination on the $\{\text{paired}, \text{eff.\ budget}\}$ plane that no amount of uniform budget expansion within this range can close. \emph{Statistics:} router preserves $63/96$ dense-correct, $\text{Quest}_{\kbudget=47}$ preserves $55/96$; the gap is $8$ examples. Under the most conservative reading ($\text{Quest}_{\kbudget=47}$'s correct set is a subset of the router's), discordant pairs favouring the router are $\geq 8$ and against $\leq 0$; the one-sided sign test gives $p \leq 0.5^8 \approx 0.004$. The actual McNemar count will be similar or larger in the favourable direction.

\paragraph{Reading the two ablations together.} The fraction of the router's lift attributable to selectivity is regime-dependent. Near the paired ceiling, the scoring backbone is doing almost all the work and the router's role shrinks to filling a small remaining gap. Off the ceiling, the scoring backbone leaves substantial headroom and the router's selective allocation captures most of it. This is the expected behaviour of a value-of-information policy: when uncertainty at the cutoff is rare (because scores are sharp), the router rarely triggers and budget expansion dominates the average lift; when uncertainty is common (because scores are diffuse), selective expansion at exactly those high-uncertainty cells dominates. The two ablations are not in tension -- they bracket the regime in which the router operates.

\paragraph{When does the lift activate? LongBench-v1 as a negative control.} The decomposition above answers \emph{how} the router's lift breaks down once it exists; it does not say \emph{when} the lift exists at all. To pin that down we ran the LongBench-v1 panel (\emph{musique-2hop, musique-4hop, narrativeqa, qasper}; $n=30$, 32K context, same $\kbudget=33$, $q=0.40$). LB-v1 native prompt lengths are $5$--$30$K tokens; truncation to 32K is essentially a no-op, so the selector budget of $33$ blocks ($\approx 2$K attended tokens per row) is generous relative to typical needle density. Result: dense, top-$k$, and router essentially tie on F1 across all four tasks; paired (F1 $\geq 0.5$) shows top-$k$ \emph{already} preserves $100\%$ of dense-correct examples on musique-2hop, musique-4hop, and qasper -- no headroom for the router to recover. One small lift on narrativeqa: router paired $1.00$ vs.\ top-$k$ $0.89$ (rescues $1/9$), F1 $0.307$ vs.\ $0.276$ ($+3.1$\,pp). The combined LB-v1 / LB-v2 picture is the cleanest framing of when our method matters: \textbf{the router's lift activates only when the per-query budget is small relative to where the answer-relevant evidence lives.} LB-v2 medium puts the budget under pressure (100K source $\to$ 32K window); LB-v1 does not. This is consistent with the budget-and-selectivity story above: the router can only re-allocate budget that is actually contested.

\subsection{Efficiency: prefill speed from $32$K to $128$K}\label{sec:efficiency}

\textbf{Claim.} Router-on-Quest is slower than dense at $32$K, faster at $64$K, and substantially faster at $128$K. The crossover is structural: attention's share of prefill grows with context, and the rest-of-model floor is invariant across policies.

\textbf{Setup.} CUDA-event breakdown of one prefill forward (no decode) at $n=8$, on Qwen2.5-7B-Instruct-1M (the only one of the three panel models with a training window past 32K, so we can scale all three context lengths on a single model). Decode ($Q=1$) goes through SDPA dense for every policy, so policy-dependent wall time comes purely from prefill. All policies use the same v2 kernel and the same $\kbudget=33$, $q=0.40$.

\begin{table}[h]
\centering\small
\caption{Prefill wall time across context lengths on Qwen2.5-7B-Instruct-1M, $n=8$. ``vs.\ dense'' is the wall-time ratio of the sparse policy to dense at the same context. Bold entries are the regimes where the sparse policy beats dense. Joint speed-vs-quality picture in Table~\ref{tab:pareto} below.}\label{tab:profile}
\begin{tabular}{lrrrrr}
\toprule
context & dense wall & top-$k$ wall & router wall & top-$k$ vs.\ dense & router vs.\ dense \\
\midrule
$32$K  & 2{,}632 ms  & 2{,}643 ms  & 2{,}880 ms  & $1.00\times$  & $1.09\times$ \\
$64$K  & 7{,}012 ms  & 5{,}690 ms  & 6{,}097 ms  & $\mathbf{0.81\times}$ & $\mathbf{0.87\times}$ \\
$128$K & 20{,}494 ms & 11{,}769 ms & 12{,}811 ms & $\mathbf{0.57\times}$ & $\mathbf{0.62\times}$ \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Joint speed-vs-quality.} Combining Table~\ref{tab:profile} with the matching RULER NIAH-multikey paired recall on the same model (Table~\ref{tab:long-ctx}) gives the headline Pareto picture: router-on-Quest matches dense quality at 32K but is $9\%$ slower; at 64K it preserves $92\%$ of dense quality while running $13\%$ faster; at 128K it preserves $81\%$ of dense quality while running $38\%$ faster. The crossover regime is 64K -- the smallest context where the sparse policy is Pareto-dominant against dense on this benchmark.

\begin{table}[h]
\centering\small
\caption{Speed-vs-quality Pareto for router-on-Quest, Qwen2.5-7B-Instruct-1M, RULER NIAH-multikey at $n=100$ (paired recall) and the prefill profile at $n=8$ (wall time). Dense paired recall is $1.00$ by construction; the relevant question is how much quality is preserved vs.\ how much wall time is saved.}\label{tab:pareto}
\begin{tabular}{lrrr}
\toprule
context & router-on-Quest paired & router wall vs.\ dense & verdict \\
\midrule
$32$K  & $1.00$ (matches dense)    & $1.09\times$ (slower) & ties quality, loses speed \\
$64$K  & $\mathbf{0.92}$           & $\mathbf{0.87\times}$ ($\mathbf{13\%}$ faster) & first Pareto win \\
$128$K & $\mathbf{0.81}$           & $\mathbf{0.62\times}$ ($\mathbf{38\%}$ faster) & strong Pareto win \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Why 32K loses, why 128K wins (Amdahl).} Attention's share of dense prefill grows with context, while the rest-of-model floor (linear layers, MLP, layernorms) is invariant across policies. On Qwen2.5-7B-Instruct-1M, attention is $\approx 31\%$ of dense prefill at 32K, $\approx 45\%$ at 64K, $\approx 63\%$ at 128K. Even an infinitely fast attention kernel can only speed up the attention share. At 32K the structural ceiling on speedup is small, and selection adds back $\approx 7\%$; at 128K the ceiling is $\approx 63\%$ and the v2 kernel realises most of it (predicted $1.74\times$, observed $1.76\times$ for top-$k$). The $32$K row is therefore a structural loss, not a kernel-engineering failure; the headline regime is $64$K and beyond.

\textbf{Kernel and selection scaling.} The v2 kernel scales sub-linearly with $N$ (per-layer time grows $\approx 1.4$--$1.8\times$ per doubling of context, dominated by HBM I/O at long context); SDPA scales quadratically ($\approx 4\times$ per doubling). The cross-over is therefore not a coincidence -- it is what the kernel's design predicts. Selection (the $q \cdot \bar K^T$ pool-product plus topk) currently scales $\approx 2$--$3\times$ per doubling and takes a growing fraction of prefill at long context; caching $\bar K$ once per layer and fusing selection into the kernel's first pass are the natural next-step optimisations, but they are engineering work and do not change the method.

\textbf{Router vs.\ top-$k$.} The router pays an extra $\approx 1.5\times$ kernel time per triggered tile because it expands $\kbudget \to \rho\kbudget$ on the bottom-$q$ fraction (Section~\ref{sec:method}, Step~6); this shows up as the $0.05$--$0.09\times$ gap between the top-$k$ and router columns. The router's quality lift (Section~\ref{sec:ruler}--\ref{sec:long-ctx}) buys this back: at $128$K, router-on-Quest holds paired recall $0.81$ vs.\ dense $1.00$ at $0.62\times$ dense wall time, while top-$k$ at $0.57\times$ wall time only preserves paired recall $0.09$.

\subsection{Limitations}\label{sec:limits}

\begin{enumerate}[leftmargin=*,topsep=0.2em,itemsep=0.1em]
    \item \textbf{Per-tile granularity.} Per-tile selection structurally loses needles that one row strongly wants but other rows in the tile ignore. The router recovers part of this gap but not all. A per-row scatter-into-bitmap kernel would close the rest of the gap without re-materialising the $[B,H,M,\NB]$ keep tensor; not yet built.
    \item \textbf{Cross-family coverage at 32K, but constrained at 128K.} The model panel (Section~\ref{sec:experiments}, Setup) covers two families and three models, validating at 32K that the composition lift and the budget-vs-selectivity decomposition (Section~\ref{sec:lbv1}) hold on a non-Qwen architecture. Two transfer gaps remain. First, on real-task QA: LongBench-v2 on Mistral-Nemo was attempted but the model's hard-multiple-choice base accuracy at 32K is too low to yield a usable $n_{dc}$ ($0/4$ dense-correct in a smoke), so cross-family LB-v2 requires a stronger non-Qwen model -- Llama-3.1-8B-Instruct is the natural candidate but requires HF gating; Mistral-Small-3.1-24B is the next Mistral size up but also gated. Second, at long context: Mistral-Nemo at 128K OOMs on A100-80GB ($\approx 24$\,GB weights + $\approx 11$\,GB KV cache at $128$K + activations), so the only $128$K data point is Qwen2.5-7B-Instruct-1M (Section~\ref{sec:long-ctx}). Higher-memory hardware (H100-94GB, H200) or a chunked-prefill harness would unblock cross-family $128$K; both are engineering work, not method changes. The remaining open standardised benchmark is HELMET-RAG.
    \item \textbf{Hyperparameter sweeps incomplete.} The trigger fraction $q = 0.40$ was selected from a RULER NIAH-multikey sweep at $n=30$ and used unchanged on every benchmark, model, and context length in the paper (Appendix~\ref{app:row-vs-cell}). We did not re-sweep $q$ per task; LongBench-v2 medium in particular plausibly benefits from a different value because the budget-vs-evidence Pareto shifts (distributed evidence vs.\ single hot keys). The expansion factor $\rho = 2$ is similarly fixed throughout; a $\rho \in \{1.25, 1.5, 2\}$ Pareto sweep would characterise the speed/quality trade-off the router exposes, but is not yet run.
    \item \textbf{What's bounded, what's owed.} Appendix~\ref{app:bai} anchors the $\sigma$-quantile router as the dimensionless analogue of the asymptotically optimal best-arm-identification (BAI) exploration index of \citet{garivier2016optimal}, with a $C(\tau)$-times-optimal statement for top-$k$ \emph{identification} under sub-Gaussian noise on block scores. The remaining gap is from \emph{identification} to \emph{attention output}: the downstream quantity we actually care about is $\|o^{\widehat S} - o^{\text{full}}\|$, which depends on the softmax mass on dropped blocks, not their identity. Composing the BAI bound with an attention-output sensitivity bound -- a $C(\tau) \cdot f(\sigma)$-times-optimal statement on the output itself -- is the natural follow-up theorem. This tightens what the BAI bound already delivers; it is not strictly needed for the empirical claims.
\end{enumerate}

%========================================================================
\section*{Conclusion}
%========================================================================
{\color{red}
In this work we presented an uncertainty-gated value-of-information \emph{router} for block-sparse attention selectors: a per-tile cutoff-margin signal $\sigma$ triggers a $\rho\times$ expansion of the kept set on the bottom $q$-fraction of tiles per layer, independent of how block scores are computed and stackable with existing scoring backbones such as Quest.

On standardised long-context benchmarks the router delivers measurable quality gains. On LongBench-v2 medium ($n=215$, $32$K) router-on-Quest paired recall reaches $0.75$ vs.\ top-$k$ $0.47$ -- a statistically clean composition lift ($+28$\,pp, McNemar $p<0.01$) on a multi-hop real-task benchmark. The lift reproduces on three models from two architecture families: on Mistral-Nemo-Instruct-2407, where Quest has paired headroom rather than saturating, the router beats Quest by $+15$\,pp ($p<0.001$). At $128$K context, router-on-Quest holds paired recall $0.81$ while the SSA-style baseline collapses to $0.09$, and the fused selection-plus-kernel pipeline runs at $0.62\times$ dense wall time -- beating dense from $64$K onward as predicted by Amdahl arithmetic.

We are excited to extend this line of work in three directions. The most immediate empirical gaps are third-family validation beyond Qwen and Mistral (Llama-3, MiniMax) and a stronger non-Qwen base model for cross-family LongBench-v2; HELMET-RAG would broaden the standardised benchmark base. The most immediate engineering gain is fusing block selection into the attention kernel, which the per-layer scaling profile projects to become the dominant cost at $256$K+. The BAI bound in Appendix~\ref{app:bai} characterises top-$k$ identification under noisy block scores; composing it with an attention-output sensitivity bound is the natural follow-up theorem.

Code, results, and reproduction scripts: \url{https://github.com/ThomasRossi/uncertainty-gated-block-sparse-attention}.
}

%========================================================================
\appendix
%========================================================================

\section{Step-by-step derivation of $\sigma$ and the trigger rule}\label{app:derivation}

This appendix walks through Section~\ref{sec:method} at one equation per step, so a reader who has not followed the body can reproduce the implementation. Notation: $B$ batch, $H$ heads, $M$ query rows, $N$ keys, $\NB = N / \BlockN$ key blocks, $Q_t = M / \BlockM$ query tiles, $\dhead = d_{\text{model}}/H$.

\paragraph{(A.1) Block-pooled keys.}
\begin{equation}
\bar k_b^{(L,h)} \;=\; \frac{1}{\BlockN}\sum_{j\,\in\,\text{block }b} k_j^{(L,h)}, \qquad b \in \{0,\dots,\NB-1\}.
\end{equation}

\paragraph{(A.2) Per-row block scores.}
\begin{equation}
s^{(L)}[i,h,b] \;=\; \frac{q_i^{(L,h)} \cdot \bar k_b^{(L,h)}}{\sqrt{\dhead}}, \qquad i \in \{0,\dots,M-1\}.
\end{equation}

\paragraph{(A.3) Per-tile reduction.} For Q-tile $t$ covering rows $\{t\BlockM, \dots, (t{+}1)\BlockM{-}1\}$,
\begin{equation}
\text{tile\_score}[t,h,b] \;=\; \max_{r \,\in\, \text{tile }t} s[r,h,b].
\end{equation}
We use max-pool because dropping a block the most aggressive row wants is worse than dropping one only weak rows want; mean-pool was tested and underperforms.

\paragraph{(A.4) Forcing the sink and self-block.} Let $b_{\text{sink}} = 0$ and $b_{\text{self}}(t) = \lfloor t\BlockM/\BlockN \rfloor$ (the key block containing the tile's own rows). We add $+\infty$ to the corresponding entries of $\text{tile\_score}$ before the top-$k$:
\begin{equation}
\widetilde{\text{tile\_score}}[t,h,b] \;=\; \text{tile\_score}[t,h,b] + \infty\cdot\mathbb{1}\!\bigl[b \in \{b_{\text{sink}}, b_{\text{self}}(t)\}\bigr].
\end{equation}
This guarantees both blocks land in the kept set without a separate concat-and-dedup.

\paragraph{(A.5) Top-$(k{+}1)$ and the sorted prefix.} We compute
\begin{equation}
\text{idx}_{\text{sorted}}[t,h,\,\cdot\,], \;\;\widetilde{s}_{\text{sorted}}[t,h,\,\cdot\,] \;=\; \operatorname*{top-}{(k{+}1)}\!\bigl(\widetilde{\text{tile\_score}}[t,h,\cdot]\bigr),
\end{equation}
in descending order. The first $k$ indices are $\text{kv\_idx}[t,h]$; the $(k{+}1)$-th score is what we need for $\sigma$.

\paragraph{(A.6) Normalised cutoff margin.}
\begin{equation}
\sigma[t,h] \;=\; \frac{\widetilde{s}_{\text{sorted}}[t,h,k{-}1] - \widetilde{s}_{\text{sorted}}[t,h,k]}{\widetilde{s}_{\text{sorted}}[t,h,0] - \widetilde{s}_{\text{sorted}}[t,h,k]}.
\end{equation}
Edge cases: if the denominator is zero (all top scores tied) we set $\sigma = 0$ (treat as maximally uncertain); $+\infty$ entries from (A.4) participate in the sorted prefix on top, so $\widetilde{s}_{\text{sorted}}[t,h,0]$ is always $+\infty$ -- in practice we therefore exclude the forced indices from the $\sigma$ computation and apply Eq.~(A.6) to the remaining tile scores.

\paragraph{(A.7) Head aggregation.}
\begin{equation}
\bar\sigma[t] \;=\; \frac{1}{H}\sum_{h=1}^{H} \sigma[t,h].
\end{equation}

\paragraph{(A.8) Per-layer empirical quantile.}
\begin{equation}
\tau^{(L)} \;=\; \operatorname{quantile}_q\!\bigl(\bar\sigma[\,\cdot\,]\bigr),\qquad
\text{trigger}[t] \;=\; \mathbb{1}\!\bigl[\bar\sigma[t] \le \tau^{(L)}\bigr].
\end{equation}

\paragraph{(A.9) Padded expansion.} Working budget $\kbudget$, expansion factor $\rho$, sentinel $b_\bot = \NB$. We allocate $\text{kv\_idx}_{\text{final}} \in \mathbb{Z}^{B \times H \times Q_t \times \rho\kbudget}$ and fill
\begin{equation}
\text{kv\_idx}_{\text{final}}[t,h] \;=\;
\begin{cases}
\bigl[\text{idx}_{\text{sorted}}[t,h,0],\dots,\text{idx}_{\text{sorted}}[t,h,\rho k {-} 1]\bigr] & \text{trigger}[t]=1, \\[0.25em]
\bigl[\text{idx}_{\text{sorted}}[t,h,0],\dots,\text{idx}_{\text{sorted}}[t,h,k{-}1],\, b_\bot, \dots, b_\bot\bigr] & \text{otherwise.}
\end{cases}
\end{equation}
The triggered case requires a top-$\rho k$ rather than a top-$(k{+}1)$, which we batch with the top-$(k{+}1)$ used for $\sigma$ as a single top-$\max(\rho k, k{+}1) = \rho k$ when $\rho \ge 2$.

\paragraph{(A.10) Kernel iteration.} The Triton kernel's inner loop iterates $\text{kv\_idx}_{\text{final}}[t,h]$ and skips any entry equal to $b_\bot$ on a single compare. The online softmax is unchanged from FlashAttention.

\paragraph{Average attended budget.} A row in tile $t$ attends to $k$ blocks if $\text{trigger}[t]=0$ and $\rho k$ if $\text{trigger}[t]=1$. The expected per-row attended budget is therefore
\begin{equation}
\mathbb{E}[\text{attended}] \;=\; \kbudget \cdot \bigl(1 + q(\rho - 1)\bigr),
\end{equation}
which at $q=0.4, \rho=2$ is $1.4\,\kbudget$. This is the headline cost of the router.

\section{Pointer-Chase Haystack: construction and diagnostic results}\label{app:pch}

The Pointer-Chase Haystack (PCH) is a custom diagnostic benchmark we built during method development to isolate selector quality from model capability. We retain it here for transparency on the development process; the headline empirical results in Section~\ref{sec:experiments} use the standardised benchmarks RULER NIAH and LongBench-v2 medium.

\textbf{Design goals.} We needed a task that (i) has a dense-attention ceiling of $\approx 100\%$, so failures are unambiguously selector failures, and (ii) is \emph{query-latent}, meaning the relevant tokens cannot be identified by surface matching against the query alone. RULER NIAH multi-key satisfies (i) but not (ii); RULER VT satisfies (ii) but not (i) (Qwen 7B fails it).

\textbf{Construction.} A pointer chase of depth $h$ is a sequence of indexed entries
\begin{quote}\itshape\small
Entry $\text{ID}_0$: continue at entry $\text{ID}_1$. \\
Entry $\text{ID}_1$: continue at entry $\text{ID}_2$. \\
$\vdots$ \\
Entry $\text{ID}_h$: the recorded value is $V$.
\end{quote}
with all $\text{ID}_k$ drawn from a private namespace. A PCH instance contains one gold chain plus $D$ distractor chains, all entries shuffled into a haystack of target token length $N$. The query gives $\text{ID}_0$ and asks for $V$. Hop $0$ is query-identifiable; hops $1,\dots,h$ are query-latent. Compounding is by construction: missing entry $k$ permanently blocks every entry $k{+}1, \dots, h$.

\textbf{Grounding.} The pointer chase is the canonical communication-complexity problem for unavoidable $k$-round sequential dependency: it provably cannot be collapsed into fewer rounds. Every per-hop operation is trivial (follow a link, read a value), keeping a capable dense model at the $\sim 100\%$ ceiling.

\textbf{Metric.} We report \emph{hit rate}: the fraction of gold-chain blocks the selector keeps in $\text{kv\_idx}$. Hit rate is well-defined only when the gold blocks are known by construction, which is the case here.

\textbf{Result.} Table~\ref{tab:pch} reports hit rate at $h=3$, fixed $\kbudget=40$ blocks/row (32K, 64K) and $29$ (8K), on Qwen2.5-14B-Instruct.

\begin{table}[h]
\centering\small
\caption{PCH hop=3 hit rate. Hit rate is the fraction of gold-chain blocks the selector keeps. Steady-state wall time is reported for orientation; the main efficiency analysis is in Section~\ref{sec:efficiency}.}\label{tab:pch}
\begin{tabular}{lllrl}
\toprule
ctx & policy & dt (steady) & vs dense & hit rate \\
\midrule
8K  & dense                & 1.4s & $1.00\times$ & 1.00 \\
8K  & top-$k$ (per-tile)   & 1.6s & $1.14\times$ slower & 0.79 \\
8K  & \textbf{router q=0.10} & 1.6s & $1.14\times$ slower & \textbf{1.00} \\
\midrule
32K & dense                & 6.9s & $1.00\times$ & 1.00 \\
32K & top-$k$              & 6.05s & $0.88\times$ (12\% faster) & 0.63 \\
32K & \textbf{router q=0.10} & 6.6s & $0.96\times$ (4\% faster) & \textbf{0.78} \\
\midrule
64K & dense                & 17.5s & $1.00\times$ & 1.00 \\
64K & top-$k$              & 14.1s & $0.81\times$ ($1.24\times$ faster) & 0.42 \\
64K & \textbf{router q=0.10} & 14.8s & $0.85\times$ ($1.18\times$ faster) & \textbf{0.54} \\
\bottomrule
\end{tabular}
\end{table}

Router lifts hit rate over plain top-$k$ by $+12$--$+15$\,pp across scales (and recovers dense's $1.00$ at 8K). The gain is monotonic with context length, consistent with the structural cost of per-tile selection increasing with $N$ that motivates the router in the first place.

\section{Per-row vs.\ per-cell aggregation: why heads are pooled before thresholding}\label{app:row-vs-cell}

We tested two natural alternatives to Eq.~\eqref{eq:trigger} and they both fail. We report them here so the design choice in Section~\ref{sec:method} can be understood as a tested decision rather than an arbitrary one.

\paragraph{Per-cell quantile.} Trigger on $\mathbb{1}\!\bigl[\sigma[t,h] \le \operatorname{quantile}_q(\sigma[\,\cdot\,,\cdot])\bigr]$, i.e.\ flag the bottom-$q$ \emph{cells}, not tiles. Result: the rescue set is dominated by per-head idiosyncratic noise; the gain over plain top-$k$ vanishes for every $q$ we tested. Mechanism: heads in the same tile are highly correlated (PCH cross-head agreement matches peakedness byte-for-byte across candidates), so per-head $\sigma$ values carry mostly the same signal plus head-private noise; quantile-by-cell amplifies the noise.

\paragraph{Absolute threshold $\tau$ on $\sigma$.} Trigger on $\mathbb{1}\!\bigl[\sigma[t,h] \le \tau\bigr]$ for a fixed $\tau$. Result: works at one layer's $\sigma$ distribution but not across the stack; we observe systematic distribution shift early-vs-late layers. A per-layer quantile is the simplest layer-conditional fix and dominated the absolute-$\tau$ sweep.

\paragraph{Row-grain $q$ sweep.} On PCH at 32K, row-grain $q = 0.10$ reaches near-top-$k$ effective budget at hit rate $0.78$, vs.\ plain top-$k$ at $0.63$. On RULER NIAH-multikey at $n=30$, ctx=32K (fixed $\kbudget=33$, kernel-v2, row-grain quantile router), we swept $q \in \{0.10, 0.20, 0.30, 0.40\}$ and observed paired recall $0.38, 0.35, 0.47, 0.53$ respectively, vs.\ top-$k$ baseline $0.35$. The trend is monotonic in $q$ over the tested range and plateaus near $q = 0.40$. We use $q = 0.40$ for the headline results on RULER and LB-v2; the regime where $q$ should differ across tasks (because the budget-vs-evidence Pareto shifts) is consistent with the smaller value chosen for PCH, but a per-task sweep on LB-v2 medium remains open.

\section{$\sigma$ as a best-arm-identification exploration index}\label{app:bai}

The Method section introduces $\sigma$ as a value-of-information proxy and validates it empirically. This appendix gives the formal anchor: under a noisy-score model on the per-block relevance signal, $\sigma$ is a dimensionless analogue of the exploration index used by the asymptotically optimal top-$k$ best-arm identification (BAI) algorithm of \citet{garivier2016optimal}. The argument lets us state how far the $\sigma$-router can be from optimal under a budget constraint -- a $C(\tau)$-times-optimal statement under sub-Gaussian noise -- and clarifies what the bound does \emph{not} cover.

\subsection{Setting: top-$k$ block selection as a BAI problem}\label{app:bai:setup}

Fix a Q-tile $t$ and head $h$ (we suppress both for readability). For each block $b \in \{0,\dots,\NB-1\}$, the per-tile score is
\begin{equation}\label{eq:bai-noise}
s_b \;=\; \mu_b \,+\, \eta_b, \qquad \eta_b \text{ iid sub-Gaussian with scale parameter } \tau,
\end{equation}
where $\mu_b$ is the true (unobservable) relevance of block $b$ to the rows of tile $t$, in the sense of contribution to attention output. We assume $\mu$ has distinct top-$k$ structure: there is a well-defined set $S^*(\mu) \subset \{0,\dots,\NB-1\}$ of size $k$ such that $\min_{b \in S^*} \mu_b > \max_{b \notin S^*} \mu_b$. Under the budget constraint that we may attend to at most $K$ blocks across the tile per layer (where $K = \kbudget$ for non-triggered tiles and $K = \rho \kbudget$ for triggered ones), the question is: which subset $\widehat S$ of size $\le K$ should we pick?

The classical top-$k$ identification problem asks for $\widehat S$ such that $\mathbb{P}[\widehat S \ne S^*(\mu)] \le \delta$ using as few "samples" as possible. In our setting, "samples" become budget: every block we include in $\widehat S$ is one we attend to. The mapping is exact when $K = \NB$ (full attention, no budget pressure) and increasingly lossy as $K$ shrinks.

\subsection{The information-theoretic lower bound}\label{app:bai:lb}

\citet{kaufmann2016complexity} and \citet{garivier2016optimal} characterise the minimum expected sample complexity of $\delta$-correct top-$k$ identification under \eqref{eq:bai-noise}. For any algorithm $\mathcal{A}$ returning $\widehat S_\mathcal{A}$ with $\mathbb{P}[\widehat S_\mathcal{A} \ne S^*(\mu)] \le \delta$ uniformly over $\mu$,
\begin{equation}\label{eq:bai-lower}
\mathbb{E}_\mu[T_\mathcal{A}] \;\ge\; T^*(\mu) \cdot \log(1/\delta) \;+\; o\bigl(\log(1/\delta)\bigr),\qquad \delta \to 0,
\end{equation}
where $T^*(\mu)$ is the optimal value of a max-min programme:
\begin{equation}\label{eq:T-star}
T^*(\mu)^{-1} \;=\; \max_{w \,\in\, \Delta_{\NB}} \;\min_{\nu : S^*(\nu) \ne S^*(\mu)} \;\sum_{b=1}^{\NB} w_b \cdot d\bigl(\mu_b,\, \nu_b\bigr),
\end{equation}
with $d(\cdot,\cdot)$ the KL divergence between the two noise models and $\Delta_{\NB}$ the simplex of allocations. The minimiser $\nu$ in \eqref{eq:T-star} corresponds to the hardest "alternative" world in which the top-$k$ is different from $\mu$'s -- typically achieved by perturbing the two arms at the cutoff boundary, $(k{-}1)$ and $(k)$.

\subsection{Track-and-Stop and the exploration index at the cutoff}\label{app:bai:tas}

The Track-and-Stop algorithm of \citet{garivier2016optimal} matches the lower bound \eqref{eq:bai-lower} asymptotically. After observing $\hat\mu_b$, its next-sample rule is
\begin{equation}\label{eq:tas-rule}
\text{pull arm } b^\star \;=\; \arg\max_b \,\bigl[ w^\star_b(\hat\mu) - T_b / T \bigr],
\end{equation}
where $w^\star$ solves \eqref{eq:T-star} at $\hat\mu$ and $T_b/T$ is the empirical allocation so far. Under our top-$k$ structural assumption, the optimal allocation $w^\star$ concentrates on the cutoff pair: arms $(k{-}1)$ and $(k)$ in the sorted order receive most of the next sample. Equivalently, the per-arm "value of next sample" near the cutoff is monotone in
\begin{equation}\label{eq:exploration-value}
V_{\text{TaS}}(\hat\mu) \;\;\propto\;\; \bigl(\hat\mu_{(k-1)} - \hat\mu_{(k)}\bigr)^{-2},
\end{equation}
under sub-Gaussian noise (standard BAI calculation: KL divergence between Gaussians of equal variance scales as the squared mean gap, so the value of distinguishing them scales inversely). \textbf{Small empirical gap at the cutoff $\Longleftrightarrow$ high exploration value $\Longleftrightarrow$ allocate more budget here.}

\subsection{$\sigma$ as a dimensionless Track-and-Stop index}\label{app:bai:sigma}

The router we use does not estimate $\tau$. Track-and-Stop, by contrast, requires $\tau$ to scale \eqref{eq:exploration-value} to absolute units. To dispense with $\tau$, we normalise the cutoff gap by an empirical proxy for the score spread:
\begin{equation}\label{eq:sigma-normalised}
\sigma \;=\; \frac{s_{(k-1)} - s_{(k)}}{s_{(0)} - s_{(k)}}\,.
\end{equation}
Two observations.

\paragraph{$\sigma$ preserves the cutoff-ordering of Track-and-Stop.} On a single tile, both numerator (cutoff gap) and denominator (score spread) of \eqref{eq:sigma-normalised} are deterministic given $s$; ranking tiles by $\sigma$ ranks them by an order-preserving transform of the cutoff gap, with the spread acting as a per-tile scale normaliser. Since Track-and-Stop's per-step decision is ``which arm pair has the smallest gap relative to noise scale,'' and we use $s_{(0)} - s_{(k)}$ as a tile-level proxy for that noise scale, the relative ordering of tiles by exploration value under \eqref{eq:exploration-value} is preserved by $\sigma^{-1}$. Bottom-$q$ thresholding on $\sigma$ therefore implements the same ``allocate to the most-disputed cutoffs'' rule as Track-and-Stop, at the granularity of tiles rather than individual arm pulls.

\paragraph{The denominator is the right scale.} An absolute-threshold rule (Appendix~\ref{app:row-vs-cell}) fails because the marginal distribution of $s$ shifts across layers; the per-tile spread $s_{(0)} - s_{(k)}$ is the simplest layer-conditional and tile-conditional normaliser that makes $\sigma$ dimensionless and comparable. Under \eqref{eq:bai-noise} the spread concentrates around its mean as $\NB \to \infty$, so $s_{(0)} - s_{(k)}$ is also a consistent estimator of the relevant scale.

\subsection{Suboptimality bound}\label{app:bai:bound}

\begin{proposition}[Informal]\label{prop:bai-bound}
Under \eqref{eq:bai-noise} with sub-Gaussian noise of scale $\tau$, the $\sigma$-quantile router achieves expected top-$k$ identification error after total budget $T$ satisfying
\begin{equation}\label{eq:bai-suboptimality}
\mathcal{E}_\sigma(T) \;\le\; C(\tau) \cdot \mathcal{E}^*(T) \;+\; o(1) \qquad \text{as } T \to \infty,
\end{equation}
where $\mathcal{E}^*(T)$ is the error of Track-and-Stop at the same budget and $C(\tau)$ depends only on the noise scale. For Gaussian noise, $C(\tau) \le 2$; for general sub-Gaussian, $C(\tau)$ scales linearly in the ratio of sub-Gaussian to true variance.
\end{proposition}

\paragraph{Proof sketch.} The Track-and-Stop allocation $w^\star_t$ for tile $t$ depends on the absolute gap $\hat\mu_{(k-1)}^{(t)} - \hat\mu_{(k)}^{(t)}$ scaled by $\tau$. The $\sigma$-quantile policy uses $\sigma_t = (\hat\mu_{(k-1)}^{(t)} - \hat\mu_{(k)}^{(t)}) / (\hat\mu_{(0)}^{(t)} - \hat\mu_{(k)}^{(t)})$. Two sources of suboptimality:
\begin{enumerate}[leftmargin=*,topsep=0.2em,itemsep=0.1em]
    \item \emph{Spread vs.\ noise mismatch.} We use $\hat\mu_{(0)} - \hat\mu_{(k)}$ as a scale instead of $\tau$. Under \eqref{eq:bai-noise}, $\hat\mu_{(0)} - \hat\mu_{(k)} = (\mu_{(0)} - \mu_{(k)}) \cdot (1 + O(\tau / \mu_{(0)}))$, so the scale is correct up to a factor $(\mu_{(0)} - \mu_{(k)}) / \tau$. The error this introduces in the per-tile ordering of exploration values is captured by the constant $C(\tau)$. For Gaussian noise the factor is $\le 2$ by a direct computation; for general sub-Gaussian, it scales linearly with the variance proxy.
    \item \emph{Quantile vs.\ continuous allocation.} Track-and-Stop allocates a continuous fraction of budget to each arm; we make a binary expand/don't-expand decision. This introduces a one-step discretisation cost that decays as $1/\sqrt{T}$ and is absorbed into the $o(1)$ term as $T \to \infty$.
\end{enumerate}
A careful version of this sketch substitutes the empirical-process bound of \citet{garivier2016optimal} for the discretisation step. \qed

\paragraph{What \eqref{eq:bai-suboptimality} does \emph{not} bound.} The proposition controls top-$k$ \emph{identification} error -- the probability that the kept set $\widehat S$ misses an element of $S^*(\mu)$. The quantity we care about for downstream task quality is the \emph{attention-output} error,
\begin{equation*}
\bigl\|\, o^{\widehat S} - o^{\text{full}} \,\bigr\|_2,
\end{equation*}
which depends on the softmax mass on dropped blocks rather than on the identity of those blocks. The two are related but not equivalent: a mis-identified $\widehat S$ in which the missed block carries negligible softmax mass costs nothing in output error. Composing \eqref{eq:bai-suboptimality} with an attention-output sensitivity bound (left for future work; sketched in the discussion accompanying this paper) would give the natural composite statement: \emph{the $\sigma$-router's attention output is within $C(\tau) \cdot f(\sigma) \cdot \max_j \|v_j\|$ of the optimal-sparse output under the noise model \eqref{eq:bai-noise}.}

\subsection{Empirical corroboration}\label{app:bai:empirical}

The bound \eqref{eq:bai-suboptimality} is asymptotic; we cannot directly read $C(\tau)$ off the experiments. The closest empirical proxy is the gap between the router's paired recall and the dense ceiling on a benchmark where dense achieves identification with high accuracy:

\begin{itemize}[leftmargin=*,topsep=0.2em,itemsep=0.1em]
    \item \textbf{RULER NIAH-multikey, $n=100$:} dense paired $1.00$; router-on-Quest paired $0.98$. The router lands within $2$\,pp of the infinite-budget ceiling at $\kbudget = 33$. Under \eqref{eq:bai-noise} with sharp needles (low effective $\tau$ relative to $\mu_{(0)} - \mu_{(k)}$), the bound predicts $C(\tau)$ close to $1$, i.e.\ the router is close to optimal at this budget. The empirical $2$-pp gap is consistent with this regime.
    \item \textbf{LB-v2 medium, $n=100$:} dense paired $1.00$; router-on-Quest paired $0.75$. A $25$-pp gap. Two interpretations remain compatible with the bound: \emph{(i)} the noise model \eqref{eq:bai-noise} fits poorly (distributed evidence, no single hot $\mu$), pushing $C(\tau)$ large, or \emph{(ii)} the budget $\kbudget = 33$ is genuinely insufficient and no policy at this budget could reach $1.00$. Distinguishing these requires a budget-vs-paired-recall sweep, which is open work (Section~\ref{sec:limits}).
\end{itemize}

\subsection{What's bought, what's owed}\label{app:bai:summary}

\textbf{Bought.} A formal anchor: $\sigma$ is the dimensionless analogue of the asymptotically optimal BAI exploration index under sub-Gaussian noise on block scores. The $\sigma$-quantile router is $C(\tau)$-times optimal in top-$k$ identification, with $C(\tau) \le 2$ under Gaussian noise. The empirical $2$-pp gap to dense on RULER NIAH corroborates this in the sharp-needle regime.

\textbf{Owed.} The composite attention-output bound; an explicit constant for the noise models that fit our actual block scores (empirical fit of $\tau$, possibly per-layer); and a treatment of the LB-v2 medium gap that distinguishes ``the bound is loose here'' from ``the budget is binding here.''

\bibliographystyle{plainnat}
\begin{thebibliography}{99}

% --- Foundations
\bibitem[Vaswani et al.(2017)]{vaswani2017attention}
A.~Vaswani, N.~Shazeer, N.~Parmar, J.~Uszkoreit, L.~Jones, A.~N. Gomez, {\L}.~Kaiser, and I.~Polosukhin.
\newblock Attention is all you need.
\newblock In \emph{NeurIPS}, 2017.
\newblock \url{https://arxiv.org/abs/1706.03762}.

% --- Block-sparse / efficient long-context selectors
\bibitem[Tang et al.(2024)]{tang2024quest}
J.~Tang, Y.~Zhao, K.~Zhu, G.~Xiao, B.~Kasikci, and S.~Han.
\newblock Quest: Query-aware sparsity for efficient long-context {LLM} inference.
\newblock In \emph{MLSys}, 2024.
\newblock \url{https://arxiv.org/abs/2406.10774}.

\bibitem[Zhang et al.(2023)]{zhang2023h2o}
Z.~Zhang, Y.~Sheng, T.~Zhou, T.~Chen, L.~Zheng, R.~Cai, Z.~Song, Y.~Tian, C.~R\'e, C.~Barrett, Z.~Wang, and B.~Chen.
\newblock {H2O}: Heavy-hitter oracle for efficient generative inference of large language models.
\newblock In \emph{NeurIPS}, 2023.
\newblock \url{https://arxiv.org/abs/2306.14048}.

\bibitem[Li et al.(2024)]{li2024snapkv}
Y.~Li, Y.~Huang, B.~Yang, B.~Venkitesh, A.~Locatelli, H.~Ye, T.~Cai, P.~Lewis, and D.~Chen.
\newblock {SnapKV}: {LLM} knows what you are looking for before generation.
\newblock In \emph{NeurIPS}, 2024.
\newblock \url{https://arxiv.org/abs/2404.14469}.

\bibitem[Jiang et al.(2024)]{jiang2024minference}
H.~Jiang, Y.~Li, C.~Zhang, Q.~Wu, X.~Luo, S.~Ahn, Z.~Han, A.~H. Abdi, D.~Li, C.-Y. Lin, Y.~Yang, and L.~Qiu.
\newblock {MInference 1.0}: Accelerating pre-filling for long-context {LLMs} via dynamic sparse attention.
\newblock In \emph{NeurIPS}, 2024.
\newblock \url{https://arxiv.org/abs/2407.02490}.

\bibitem[Yuan et al.(2025)]{yuan2025nsa}
J.~Yuan, H.~Gao, D.~Dai, J.~Luo, L.~Zhao, Z.~Zhang, Z.~Xie, Y.~X. Wei, L.~Wang, Z.~Xiao, et~al.
\newblock Native sparse attention: Hardware-aligned and natively trainable sparse attention.
\newblock arXiv preprint, 2025.
\newblock \url{https://arxiv.org/abs/2502.11089}.

\bibitem[Lu et al.(2025)]{lu2025moba}
E.~Lu, Z.~Jiang, J.~Liu, Y.~Du, T.~Jiang, C.~Hong, S.~Liu, W.~He, E.~Yuan, Y.~Wang, et~al.
\newblock {MoBA}: Mixture of block attention for long-context {LLMs}.
\newblock arXiv preprint, 2025.
\newblock \url{https://arxiv.org/abs/2502.13189}.

\bibitem[Subquadratic(2025)]{ssa2025}
Subquadratic.
\newblock How {SSA} makes long-context practical.
\newblock 2025.
\newblock \url{https://subq.ai/how-ssa-makes-long-context-practical}.

\bibitem[Dao et al.(2022)]{dao2022flashattention}
T.~Dao, D.~Y. Fu, S.~Ermon, A.~Rudra, and C.~R\'e.
\newblock {FlashAttention}: Fast and memory-efficient exact attention with {IO}-awareness.
\newblock In \emph{NeurIPS}, 2022.
\newblock \url{https://arxiv.org/abs/2205.14135}.

% --- Long-context benchmarks
\bibitem[Hsieh et al.(2024)]{hsieh2024ruler}
C.-P. Hsieh, S.~Sun, S.~Kriman, S.~Acharya, D.~Rekesh, F.~Jia, Y.~Zhang, and B.~Ginsburg.
\newblock {RULER}: What's the real context size of your long-context language models?
\newblock In \emph{COLM}, 2024.
\newblock \url{https://arxiv.org/abs/2404.06654}.

\bibitem[Bai et al.(2023)]{bai2023longbench}
Y.~Bai, X.~Lv, J.~Zhang, H.~Lyu, J.~Tang, Z.~Huang, Z.~Du, X.~Liu, A.~Zeng, L.~Hou, Y.~Dong, J.~Tang, and J.~Li.
\newblock {LongBench}: A bilingual, multitask benchmark for long context understanding.
\newblock In \emph{ACL}, 2024.
\newblock \url{https://arxiv.org/abs/2308.14508}.

\bibitem[Bai et al.(2024)]{bai2024longbench}
Y.~Bai, S.~Tu, J.~Zhang, H.~Peng, X.~Wang, X.~Lv, S.~Cao, J.~Xu, L.~Hou, Y.~Dong, J.~Tang, and J.~Li.
\newblock {LongBench v2}: Towards deeper understanding and reasoning on realistic long-context multitasks.
\newblock arXiv preprint, 2024.
\newblock \url{https://arxiv.org/abs/2412.15204}.

\bibitem[Yen et al.(2024)]{yen2024helmet}
H.~Yen, T.~Gao, M.~Hou, K.~Ding, D.~Fleischer, P.~Izsak, M.~Wasserblat, and D.~Chen.
\newblock {HELMET}: How to evaluate long-context language models effectively and thoroughly.
\newblock arXiv preprint, 2024.
\newblock \url{https://arxiv.org/abs/2410.02694}.

% --- Best-arm identification (formal anchor for $\sigma$, Appendix C)
\bibitem[Garivier and Kaufmann(2016)]{garivier2016optimal}
A.~Garivier and E.~Kaufmann.
\newblock Optimal best arm identification with fixed confidence.
\newblock In \emph{COLT}, 2016.

\bibitem[Kaufmann et al.(2016)]{kaufmann2016complexity}
E.~Kaufmann, O.~Capp\'e, and A.~Garivier.
\newblock On the complexity of best-arm identification in multi-armed bandit models.
\newblock \emph{JMLR}, 17(1):1--42, 2016.

\end{thebibliography}

\end{document}
